# Django Framework - LTS version with security patches
Django>=5.1,<5.2

# Image Processing
Pillow>=11.0.0

# Django Extensions
django-filter>=24.1
django-ratelimit>=4.1.0
django-storages>=1.14.0

# Production Server
gunicorn>=23.0.0
whitenoise>=6.6.0

# DynamoDB ORM - Replaces PostgreSQL
pynamodb>=5.5.1
# boto3>=1.26.0  # (included with pynamodb)

# Configuration
python-decouple>=3.8
python-dotenv>=1.0.0
django-environ>=0.11.2
dj-database-url>=2.0.0

# Email
resend>=0.10.0

# Security - HTML Sanitization
bleach>=6.1.0

# AWS S3 Storage
boto3>=1.26.0

# Development only (remove for production or use separate requirements-dev.txt)
# Faker>=20.0.0